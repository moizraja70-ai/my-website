{
    "name": "dentedge-website",
    "version": "1.0.0",
    "description": "Deployment repo for DentEdge",
    "scripts": {
        "deploy": "wrangler pages deploy dist"
    },
    "dependencies": {
        "openai": "^4.0.0"
    },
    "devDependencies": {
        "wrangler": "^4.64.0"
    }
}
